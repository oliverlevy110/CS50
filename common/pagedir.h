/* Oliver Levy, CS50, TSE common
 * 
 * pagedir.h
 * A module to control the functions associated with the directory of pages produced by crawler and used by indexer. 
 */

#include "../libcs50/webpage.h"

/* A function that checks to see whether the path name inputed is a crawler directory.  
 */
int is_crawler_directory(char* path);
